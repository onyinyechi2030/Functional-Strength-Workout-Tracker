# Strength Workout Tracker Cloud v11.4

GitHub Pages single-file build. Upload `index.html`, `.nojekyll`, `README.md`, and `firestore.rules` directly to the repository root.

Cloud Sync now accepts the complete Firebase configuration block in one paste field.
